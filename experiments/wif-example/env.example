# GCP WIF Example - Environment Configuration
# 
# Instructions:
#   1. Copy this file: cp env.example .env
#   2. Edit .env with your actual values
#   3. Source it: source .env
#   4. Run the setup: ./setup-wif-example-gcp.sh
#
# Note: .env is gitignored and won't be committed

# ============================================================================
# REQUIRED SETTINGS
# ============================================================================

# Your GCP Project ID
export GCP_PROJECT_ID="my-gcp-project"

# Your HyperShift cluster/infrastructure ID
export HYPERSHIFT_INFRA_ID="my-cluster"

# Path to JWKS file from your hosted cluster's service account signing key
# This is REQUIRED - see hosted-cluster-setup/README.md for how to generate it
export JWKS_FILE="hosted-cluster-setup/jwks.json"

# ============================================================================
# OPTIONAL SETTINGS
# ============================================================================

# GCP Service Account name (default: wif-app)
# export GCP_SA_NAME="wif-app"

# Kubernetes Service Account name that will be created in hosted cluster
# (default: gcp-workload-sa)
# export K8S_SA_NAME="gcp-workload-sa"

# Kubernetes Namespace where the SA will be created (default: default)
# export K8S_NAMESPACE="default"

# IAM Roles to grant to the GCP service account (comma-separated)
# Default: roles/compute.viewer
# export GCP_IAM_ROLES="roles/compute.viewer"

# ============================================================================
# EXAMPLE ROLE CONFIGURATIONS
# ============================================================================
# Uncomment one of these based on your use case:

# For compute operations (VM management, networking):
# export GCP_IAM_ROLES="roles/compute.instanceAdmin.v1,roles/compute.networkAdmin,roles/compute.securityAdmin"

# For storage operations:
# export GCP_IAM_ROLES="roles/storage.objectAdmin,roles/storage.bucketAdmin"

# For monitoring and logging:
# export GCP_IAM_ROLES="roles/monitoring.metricWriter,roles/logging.logWriter"

# For read-only access:
# export GCP_IAM_ROLES="roles/compute.viewer,roles/storage.objectViewer"

# For a comprehensive application (compute + storage + monitoring):
# export GCP_IAM_ROLES="roles/compute.viewer,roles/storage.objectViewer,roles/monitoring.metricWriter"

# ============================================================================
# VERIFICATION
# ============================================================================
# After sourcing this file, verify with:
#   echo "Project: $GCP_PROJECT_ID"
#   echo "Cluster: $HYPERSHIFT_INFRA_ID"
#   echo "JWKS File: $JWKS_FILE"

