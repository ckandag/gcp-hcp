apiVersion: v1
kind: ConfigMap
metadata:
  name: webhook-binary
  namespace: hypershift-webhooks
binaryData:
  webhook: !!binary |
    # This would contain the compiled webhook binary
    # For this demo, we'll use a simple shell script approach instead