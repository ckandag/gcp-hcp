apiVersion: v2
name: signing-key-adapter
description: Signing Key adapter - generates RSA signing keypair on management cluster and publishes JWKS to OIDC issuer bucket
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: hyperfleet-adapter
    version: "2.0.0"
    repository: ""
